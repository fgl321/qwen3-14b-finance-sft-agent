# Security Policy

请不要在公开 Issue、日志或提交中粘贴 API Key、SSH 私钥、密码、个人金融信息或内部集群地址。

运行前请复制 `agent/.env.example` 为 `agent/.env`，真实密钥只保存在本地。模型服务默认只监听 `127.0.0.1`；如需暴露到网络，必须增加 TLS、强认证、速率限制、访问日志脱敏和网络访问控制。

若发现密钥泄露，请立即在对应服务商撤销并轮换密钥，再清理 Git 历史；仅删除当前文件不足以消除历史泄露。
